import MoviesController from "./movies.controller";

describe('MoviesController', () => {

  describe('createMovie', () => {
    it ('returns success response for no errors', () => {
      expect(2).toBe(2)
    })
  
    it ('', () => {
      expect(2).toBe(2)
    })
  })
})