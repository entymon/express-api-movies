describe('MovieRepository', () => {
  describe('saveMovie', () => {
    it('throw an error for already added title', () => {
      expect(1).toBe(1)
    })

    it('add new movie', () => {
      expect(1).toBe(1)
      // TODO: remove after test
    })
  })
})